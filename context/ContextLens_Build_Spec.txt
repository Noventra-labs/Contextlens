Yes — below is a build-ready product spec for **ContextLens**, including features, system design, data model, APIs, UX, delivery plan, and acceptance criteria. The core architecture is realistic because VS Code extensions can provide both sidebar tree views and fully custom webviews, while Firebase AI Logic supports multi-turn Gemini interactions, structured outputs, and production protections like per-user rate limits and App Check.

## Product scope

ContextLens should be positioned as an **AI workflow memory layer** for coding, not as another chatbot or IDE replacement. The product should make every meaningful AI-assisted coding action traceable, reviewable, and reusable across a branch, a task, and eventually a team.

### Product definition

| Item | Spec |
|---|---|
| Product name | ContextLens |
| One-line pitch | “A Git-aware AI context layer that turns coding sessions into searchable episodes.” |
| Primary users | AI-heavy developers, indie hackers, student teams, tech leads reviewing AI-generated changes |
| Core job to be done | “Help me remember what the AI changed, why it changed it, and how to explain that to myself or my reviewer.” |
| MVP surfaces | VS Code extension + web dashboard |
| Core differentiator | Persistent episode timeline linked to git state, prompts, responses, and summaries |
| Non-goals for MVP | Full IDE replacement, team collaboration suite, GitHub App, JetBrains plugin, mobile app |

### Product goals

- Reduce context loss during AI-assisted coding.
- Make branch evolution understandable in under 60 seconds.
- Help developers generate review-ready explanations from actual coding history.
- Create a demo that instantly looks technical, useful, and premium.

### MVP success criteria

- A developer can create an episode, send an AI request, and see it logged with branch + file + diff.
- A developer can open the dashboard and inspect the timeline of calls in that episode.
- A developer can click “Explain Diff” and get a readable summary with risks and next checks.
- A developer can click “Summarize Branch” and get a PR-style overview.

---

## Features and UX

A sidebar state panel is straightforward in VS Code because the platform explicitly supports custom tree views in the sidebar, and richer interactive panes can be built with webviews. That means the best MVP UI is a lightweight tree-based “state header” plus one richer webview for chat and detailed interaction.

### Feature set

| Feature | User value | MVP spec | Stretch spec |
|---|---|---|---|
| Episode management | Organizes work into task-sized chunks | Auto-create episode on branch switch, rename episode, close episode, persistent task tracking | AI-suggested episode summaries based on code changes |
| AI chat logging | Preserves AI decisions | Built-in Gemini chat panel; every request logs metadata | Log external AI calls from pasted prompt/response |
| Git context capture | Anchors AI output to real code state | Capture branch, active file, diff snapshot at request time | Capture changed file list and commit hashes |
| Intent tagging | Makes timeline readable | Optional short intent field before/after each call | Auto-classify intent from prompt |
| State header | Gives live situational awareness | Current episode, branch, call count, AI-owned files | “Suggested next action” card |
| Episode timeline | Makes history searchable | Dashboard list of episodes with expandable calls | Timeline heatmap, branch analytics |
| Explain Diff | Helps self-review and code review | Gemini summary + risks + checks for selected diff | Inline test suggestions |
| Branch summary | Converts work into presentation-ready narrative | PR summary, key changes, risk notes | Markdown export directly to GitHub |
| Search and filter | Makes history useful later | Search by branch, file, intent, date | Search by semantic similarity |
| Auth and projects | Separates repos/workspaces | Google sign-in, project-per-repo | Org/workspace sharing |

### VS Code extension UX

| Surface | What it shows | Actions |
|---|---|---|
| Activity bar icon | ContextLens entry point | Open sidebar |
| Sidebar panel 1: State Header | Active episode (branch-linked), call count, AI-owned files, sync status | Rename episode, close episode, open dashboard, explain current diff |
| Sidebar panel 2: Gemini Chat | Prompt box, recent thread, intent tag field | Send prompt, retry, save note |
| Status bar | `ContextLens: {episode} · {n} calls (Syncing...)` | Click to focus panel |
| Command palette | Power-user control | Rename Episode, Close Episode, Explain Diff, Summarize Branch, Force Sync |

### Dashboard screens

| Screen | Purpose | Must-have components |
|---|---|---|
| Home | Overview | Projects list, recent episodes, latest branch summaries |
| Project page | Main work view | Sidebar with branches, episode timeline, search/filter |
| Episode detail | Inspection | Prompt, response, diff, metadata, explain output |
| Branch page | Review prep | Branch summary, ordered episodes, copy-as-markdown |
| Settings | Setup | Project name, repo URL, model selection, privacy toggles |

### User flows

#### 1) Autonomous episode flow
1. Extension detects current git branch on activation or switch.
2. `EpisodeStore` automatically resolves the episode ID for that branch or creates a new one.
3. `SyncEngine` starts the background interval for telemetry flushing.

#### 2) AI request flow
1. Developer opens chat in the sidebar.
2. Types prompt and optional intent tag.
3. Extension captures current branch, active file, and diff snapshot.
4. Call is pushed to the `SyncEngine` buffer.
5. `SyncEngine` flushes the log to the backend asynchronously; state header updates call count.

#### 3) Explain diff flow
1. Developer opens an episode or current state.
2. Clicks `Explain Diff`.
3. Backend assembles diff + intent + file context.
4. Gemini returns a review-style explanation.
5. Result is stored and shown in both the extension and dashboard.

#### 4) Branch summary flow
1. User opens a branch in the dashboard.
2. Clicks `Summarize Branch`.
3. Backend aggregates episode summaries and major calls.
4. Gemini outputs PR-ready text with risks and follow-up checks.

---

## System design

Firebase AI Logic supports multimodal prompting, multi-turn chat, structured output, and configurable per-user rate limits, so it fits both the interactive IDE chat path and the summary-generation endpoints. Firebase AI Logic also keeps the Gemini API key on Google-managed servers rather than exposing it to the client, and App Check adds an extra abuse-protection layer for production hardening.

### Architecture

| Layer | Tech | Responsibility |
|---|---|---|
| IDE client | VS Code extension, TypeScript | Autonomous Watchers, SyncEngine (Buffer), Sidebar/Webview |
| Web app | Next.js + Firebase Hosting | Timeline, search, episode detail, branch summary |
| Auth | Firebase Auth | Sign-in with Google |
| Database | Firestore | Projects, episodes, calls, summaries, settings |
| Backend | Firebase Functions or Cloud Run | Logging, summarization, aggregation, model proxy |
| AI layer | Gemini via Firebase AI Logic | Chat, explain diff, branch summary, structured outputs |
| Optional protection | Firebase App Check | Anti-abuse, production hardening |

### Recommended repo structure

```text
contextlens/
  apps/
    extension/           # VS Code extension
    dashboard/           # Next.js app
  services/
    api/                 # Firebase Functions or Cloud Run handlers
  packages/
    shared-types/        # TS types for Episode, Call, Summary
    prompt-templates/    # Gemini prompts
    ui/                  # Shared UI tokens if needed
  docs/
    architecture/
    demo/
    product/
```

### Extension module breakdown

| Module | Responsibility |
|---|---|
| `extension.ts` | Activation, command registration, lifecycle |
| `auth.ts` | Sign-in, token retrieval, session persistence |
| `episodeStore.ts` | Branch-bound episode state, resolution, creation |
| `syncEngine.ts` | FIFO buffer, background flush (30s), retry logic |
| `watchers.ts` | .git/HEAD (branch), .git/COMMIT_EDITMSG (commit), File Save hooks |
| `gitContext.ts` | Branch detection, diff capture, file list |
| `chatViewProvider.ts` | Webview chat UI |
| `stateTreeProvider.ts` | Sidebar tree view |
| `apiClient.ts` | Calls backend endpoints |
| `telemetry.ts` | Local timing/events |
| `redaction.ts` | Secret detection before upload |

### Backend services

| Endpoint | Method | Purpose | Input | Output |
|---|---|---|---|---|
| `/projects/create` | POST | Create project | repo name, repo URL | project id |
| `/episodes/create` | POST | Start episode | project id, label, branch | episode id |
| `/calls/log` | POST | Log AI call | project id, episode id, prompt, context | stored call + model output |
| `/episodes/explain` | POST | Explain accumulated diff | episode id | summary, risks, checks |
| `/branches/summarize` | POST | Summarize branch | project id, branch | PR summary |
| `/search` | POST | Search timeline | query, filters | episodes/calls |
| `/episodes/close` | POST | Close episode | episode id | status updated |

### AI task design

| Task | Model mode | Output shape |
|---|---|---|
| IDE chat | Fast interactive Gemini model | plain text + citations/notes if needed |
| Explain diff | Higher-quality reasoning model | JSON: `summary`, `risks[]`, `checks[]` |
| Episode summary | Reasoning model | JSON: `what_changed`, `why`, `files[]` |
| Branch summary | Reasoning model | JSON: `pr_summary`, `key_changes[]`, `review_risks[]` |
| Intent labeling | Fast model or rule-based | short label string |

### Key engineering decisions

- Use **structured JSON outputs** from Gemini for all summary endpoints.
- Treat the extension as a thin client; keep AI prompts and aggregation logic on the server.
- Cache summaries by `episodeId + diffHash` so repeated clicks are instant.
- Upload only bounded context per request; do not send the whole repository.
- Build for one IDE first: VS Code only.

---

## Data model and technical specs

Firestore organizes data hierarchically, and security rules apply only to the exact matched path, not automatically to subcollections, so the schema and rules must explicitly cover nested episode and call collections. That makes a user-scoped hierarchy like `users/{uid}/projects/{projectId}/episodes/{episodeId}/calls/{callId}` the safest default for the MVP.

### Firestore schema

```text
users/{uid}
  displayName
  email
  photoURL
  createdAt

users/{uid}/projects/{projectId}
  name
  repoUrl
  localWorkspaceName
  defaultBranch
  createdAt
  updatedAt
  settings:
    preferredModel
    redactionEnabled
    autoSummariesEnabled

users/{uid}/projects/{projectId}/episodes/{episodeId}
  label
  branchName
  status                # active | closed
  startedAt
  endedAt
  callCount
  changedFiles[]
  latestDiffHash
  manualNotes
  episodeSummary
  explainDiffSummary
  explainDiffRisks[]
  explainDiffChecks[]

users/{uid}/projects/{projectId}/episodes/{episodeId}/calls/{callId}
  createdAt
  source                # extension_chat | manual_log
  intentTag
  promptText
  modelName
  modelResponse
  branchName
  activeFilePath
  relatedFiles[]
  diffSnapshot
  diffHash
  todoMatches[]
  latencyMs
  tokenUsage:
    input
    output
  status                # success | failed | redacted
```

### Local extension state

| Key | Type | Purpose |
|---|---|---|
| `activeProjectId` | string | Current repo mapping |
| `activeEpisodeId` | string | Current task context (branch-linked) |
| `syncBuffer` | array | FIFO queue for unsynced logs |
| `syncStatus` | enum | idle | syncing | error |
| `authSession` | secure token/session data | Auth reuse |
| `draftIntent` | string | Optional convenience field |
| `lastDiffHash` | string | De-duplicate repeated logs |
| `queuedRequests` | array | Retry on network failure |

### Suggested security rules shape

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;

      match /projects/{projectId} {
        allow read, write: if request.auth != null && request.auth.uid == uid;

        match /episodes/{episodeId} {
          allow read, write: if request.auth != null && request.auth.uid == uid;

          match /calls/{callId} {
            allow read, write: if request.auth != null && request.auth.uid == uid;
          }
        }
      }
    }
  }
}
```

### API payload contracts

#### `POST /calls/log`
```json
{
  "projectId": "proj_123",
  "episodeId": "ep_456",
  "intentTag": "Refactor auth middleware",
  "promptText": "Refactor this auth flow into reusable helpers",
  "context": {
    "branchName": "feature/auth-v2",
    "activeFilePath": "src/middleware/auth.ts",
    "relatedFiles": ["src/utils/token.ts"],
    "diffSnapshot": "diff --git ...",
    "todoMatches": ["TODO: add rate limit"]
  }
}
```

#### Response
```json
{
  "callId": "call_789",
  "modelName": "gemini",
  "modelResponse": "Here is a refactor...",
  "latencyMs": 1820,
  "saved": true
}
```

#### `POST /episodes/explain`
```json
{
  "projectId": "proj_123",
  "episodeId": "ep_456"
}
```

#### Response
```json
{
  "summary": "This episode extracts token validation...",
  "risks": [
    "Possible regression in expired token handling"
  ],
  "checks": [
    "Add tests for missing Authorization header"
  ]
}
```

### Prompt templates

#### Explain Diff prompt
```text
You are a senior software engineer reviewing an AI-assisted code change.

Inputs:
- Episode label: {label}
- Branch: {branchName}
- Intents: {intentTags}
- Changed files: {files}
- Unified diff: {diff}

Return strict JSON:
{
  "summary": "2-4 sentence explanation",
  "risks": ["risk 1", "risk 2"],
  "checks": ["check 1", "check 2", "check 3"]
}

Focus on:
1. What changed
2. Why it likely changed
3. What might break
4. What should be tested
```

#### Branch Summary prompt
```text
You are preparing a pull request summary from an ordered sequence of AI-assisted coding episodes.

Inputs:
- Branch: {branchName}
- Episode summaries: {episodes}
- Changed files: {changedFiles}

Return strict JSON:
{
  "pr_summary": "3-5 sentences",
  "key_changes": ["...", "..."],
  "review_risks": ["...", "..."]
}
```

### Error states to handle

| Error | Extension behavior | Dashboard behavior |
|---|---|---|
| Not a git repo | Disable diff capture, allow manual logging | Show “No git context available” |
| No active episode | Prompt to create episode before chat | Highlight missing context |
| AI request timeout | Retry button, keep request draft | Show pending state |
| Firestore write fail | Queue locally and retry | Mark event as unsynced |
| Model quota exceeded | Fallback message, suggest retry later | Disable summary actions temporarily |
| Secret detected in diff | Ask user to redact or skip upload | Mark call as redacted |

---

## Build plan and acceptance criteria

Firestore best-practice guidance emphasizes rules and data-model planning up front, so the fastest way to ship this is to lock the schema, security rules, and endpoint contracts before polishing UI. You should also keep the model layer configurable because Firebase AI Logic’s supported-model lineup changes over time, including published shutdown notices for older Gemini variants.

### Delivery phases

| Phase | Goal | Output |
|---|---|---|
| Phase 1 | Foundation | Firebase project, auth, Firestore schema, extension skeleton |
| Phase 2 | Core logging | Create episode, log call, capture branch/file/diff |
| Phase 3 | AI value loop | Explain Diff and Branch Summary working end-to-end |
| Phase 4 | Dashboard | Timeline, filters, episode detail, branch page |
| Phase 5 | Polish | dark mode, loading states, redaction, demo data |
| Phase 6 | Submission prep | README, architecture diagram, demo video, screenshots |

### Engineering backlog

#### Must-build
- VS Code sidebar tree view
- VS Code chat webview
- Google sign-in
- Project creation
- Episode creation/close
- Diff capture
- Firestore logging
- Episode detail page
- Explain Diff endpoint
- Branch Summary endpoint
- Search by branch/date
- Demo seed data

#### Should-build
- Secret redaction
- Request queue/retry
- Copy-as-Markdown PR summary
- Keyboard shortcuts
- Cached summary responses
- Filter by file path

#### Nice-to-have
- Streaming AI responses
- Semantic search
- Git commit linking
- PR export to GitHub
- Team sharing
- Android Studio plugin later

### Acceptance criteria

| Area | Acceptance test |
|---|---|
| Auth | User can sign in and land on both extension and dashboard |
| Project setup | Repo maps to a project in under 30 seconds |
| Episode logging | New AI call appears in dashboard within 2 seconds after completion |
| Diff capture | Stored diff matches repo state at request time |
| Explain Diff | Returns summary, risks, checks for an episode with at least one diff |
| Branch Summary | Produces PR-style text from multiple episodes |
| Search | Query returns matching episodes by label/branch/file |
| Reliability | Failed write retries without losing prompt text |
| UX | All key actions reachable in ≤2 clicks from the sidebar |
| Demo readiness | End-to-end flow works on seeded data with no manual DB edits |

### What the final product package should contain

- Working VS Code extension
- Deployed dashboard
- Firebase backend
- README with setup and architecture
- Demo account with preloaded episodes
- 2-minute demo video
- 6–8 polished screenshots
- Pitch copy: problem, solution, why now, Google stack, roadmap

### Component and Workflow Details

#### 1. Authentication & Session Flow
The system uses a secure VS Code → Browser → Backend → VS Code loop to manage Firebase credentials without embedding client-side SDKs in the extension.
- **Trigger**: `AuthManager.signIn()` generates a redirect URI using `vscode.env.uriScheme` (e.g., `vscode://noventra-Labs.contextlens`).
- **Browser Hand-off**: Opens `${API_BASE}/auth/login` in the user's default browser.
- **Backend Role**: Handles OAuth with Firebase Auth (Google/GitHub). On success, it generates a **Firebase Custom Token**.
- **URI Callback**: Redirects the browser to the VS Code URI handler with `uid` and `token` (custom token).
- **Token Exchange**: `AuthManager` receives the custom token and calls the Google Identity Toolkit API (via the backend proxy) to exchange it for a **Firebase ID Token** and **Refresh Token**.
- **Storage**: Tokens are stored in `vscode.ExtensionContext.secrets`.
- **API Security**: `ApiClient` uses an Axios interceptor to inject `Authorization: Bearer <ID_TOKEN>` into every request.
- **Persistence**: `AuthManager.tryRefreshToken()` handles automatic 401 recovery using the stored refresh token.

#### 2. Project Auto-Resolution
ContextLens aims for zero-config setup by deriving project identity from the workspace environment.
- **Git Context**: `EpisodeStore.ensureProject()` triggers `GitContext.getContext()` to check for `.git`.
- **Remote Mapping**: Runs `git remote get-url origin` to identify the repository URL.
- **Backend Upsert**: Sends `folderName` and `repoUrl` to `/projects/create`. The backend uses the `repoUrl` as a unique key (or fallback to folder name) to ensure the same project is used across different machines.
- **Local Cache**: The resulting `projectId` is saved in `workspaceState` for instant resolution on subsequent activations.

#### 3. Data Capture & SyncEngine
The extension uses a "Fire and Forget" architecture to ensure IDE performance isn't degraded by network latency.
- **Watchers**: 
    - **Branch Watcher**: Polls/watches `.git/HEAD` for branch switches.
    - **Save Watcher**: Captures file paths on `onDidSaveTextDocument`.
- **SyncEngine Durability**: 
    - Every `enqueue()` call immediately writes to `cl_queue.json` in the extension's global storage.
    - Even if VS Code is killed, pending logs are reloaded and flushed on next start.
- **Flush Strategy**: A background interval (default 30s) flushes the queue in batches of 5.
- **Temp-ID Reconciliation**: `autoCreateEpisode` generates a `temp-TIMESTAMP` ID. The `SyncEngine` sends this to the backend, which maps the temporary ID to a persistent Firestore ID, allowing subsequent `call` logs to link correctly even before the episode creation is fully confirmed.

#### 4. CLI Tooling (`contextlens-cli.js`)
The CLI provides a terminal-native way to interact with the ContextLens ecosystem.
- **Initialization**: `init` creates a `.contextlens/` directory and a `config.json` file. It automatically adds the directory to `.gitignore`.
- **Configuration**: `config [key] [value]` manages local settings like `api_url` or `project_id`.
- **Sync Stub**: Designed to mirror the extension's `SyncEngine`, allowing developers to log terminal sessions or CI/CD events into the same episode timeline.

#### 5. AI Interaction (Firebase AI Logic)
- **Explain Diff**: Triggered via command palette or sidebar. It sends the current diff and episode context to the backend. The backend proxies to Gemini with a "Senior Reviewer" system prompt, returning a structured JSON object.
- **Chat Webview**: A React-based webview in the sidebar provides the chat interface. It communicates with the extension via `postMessage`, which then routes calls through the `SyncEngine` for logging.
- **Branch Summarization**: Performed on the Dashboard. It aggregates all episodes in a branch and generates a PR-ready narrative.

---

### Premium polish that is cheap to build

- Nice branch and file badges
- Read-only Monaco diff viewer
- Skeleton loaders while Gemini runs
- Empty states with one clear CTA
- Copy buttons for summaries
- Command palette shortcuts
- “AI-owned files” vs “You-owned files” visual split

#### 6. Dashboard Authentication & State
- **Account Linking**: The dashboard handles cases where a user might sign in with Google but later tries GitHub with the same email. It uses `fetchSignInMethodsForEmail` and `linkWithCredential` to consolidate identities into a single UID, ensuring the VS Code extension's token remains valid regardless of the provider used in the dashboard.
- **Reactive State**: Uses Firebase Auth listeners to protect routes and a central `AuthContext` to provide the current user and ID tokens to the API layer.
- **Project Scope**: All data fetching on the dashboard is scoped to the `auth.currentUser.uid`, enforcing multi-tenancy at the frontend layer before hitting Firestore security rules.
